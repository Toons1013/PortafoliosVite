<section class = "tarjeta">
    {{#each imgs}}
    <div class="tarjetacont">
        <img src="{{url}}" alt="{{title}}">
        <h2>{{title}}</h2>
        <p>{{description}}</p>
    </div>
    {{/each}}
    </section>
